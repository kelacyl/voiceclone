# CosyVoice Python inference dependencies
# Pick one:
#
#   GPU: pip install -r requirements-gpu.txt   (NVIDIA CUDA 12.1, ~2.5GB)
#   CPU: pip install -r requirements-cpu.txt   (any machine, ~500MB)
#
# Python 3.10 recommended. Python 3.12 may need pre-installed setuptools:
#   pip install --upgrade setuptools pip wheel
#   pip install -r requirements-cpu.txt
#
# Packaging:
#   python scripts/build_python.py --gpu
#   python scripts/build_python.py --cpu
